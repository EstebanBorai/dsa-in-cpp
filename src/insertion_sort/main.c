#include <stdio.h>

void insertion_sort(char* A, int n) {
  for (int i = 0; i < n; i++) {
    char current = A[i];
    int before = i - 1;


  }
}

int main() {
  char* characters = "Hello";

  insertion_sort(characters, 5);
}
